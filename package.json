{
  "name": "opencode-go-to-codex-api",
  "version": "1.0.0",
  "description": "API proxy that converts OpenCode Go API to Codex (OpenAI-compatible) API format",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "keywords": ["opencode", "codex", "api", "proxy"],
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5"
  }
}
